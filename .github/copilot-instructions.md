# ParentBud Web

React + TypeScript + Vite starter with TailwindCSS and shadcn/ui-ready aliases.

## Notes

- Use `npm run dev` for local development.
- Use `npm run build` to typecheck and produce the production bundle.
- The project is ready for components from shadcn/ui, Aceternity UI, Uiverse, and React Bits.
